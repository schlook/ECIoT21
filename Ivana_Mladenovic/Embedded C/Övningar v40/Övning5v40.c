#include <stdio.h>
int main(void)
{
    int val;
    int pnr;
    printf("Är du en kvinna eller en man? Tryck 0 för kvinna och 1 för man!!");
    scanf("%d", &val);
    printf("Skriv ditt prsonnummer (YYMMDDXXXX) utan bindestreck!");
    scanf("%d", &pnr);
    

    
    return 0;
}